#include "hash.h" int hash(char *chave) {     int i, n = 0;     for (i = 0; i < strlen(chave); i++)     n = (256   * n + chave[i]) % MAX;     return n; } p_hash criar_hash() { 	int i;     p_hash t;     t = malloc(sizeof(Hash));     for(i=0;i<MAX;i++){         t->vetor[i] = NULL;     }     return t; } void inserir(p_hash t, char *chave){     p_no x;     int n = hash(chave);     x = malloc(sizeof(No));     strcpy(x->chave,chave);     x->prox = t->vetor[n];     t->vetor[n] = x; } void buscar(p_hash t, char *chave){     int n,i;     p_no c;     n = hash(chave);     if(t->vetor[n] == NULL){         inserir(t,chave);         printf("%d %s\n",0,chave);     }else{         i=0;         c = t->vetor[n];         while(c!=NULL){             if(strcmp(c->chave,chave)==0){                 printf("%d %s\n",1,chave);                 i=i+1;                 break;             }             c=c->prox;         }         if(i==0){             inserir(t,chave);             printf("%d %s\n",0,chave);         }     } } void destruir_hash(p_hash t){     int i;     for(i=0;i<MAX;i++){         destruir_lista(t->vetor[i]);     }     free(t); } void destruir_lista(p_no lista){     if(lista!=NULL){         destruir_lista(lista->prox);         free(lista);     } }
